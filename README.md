# Multi-Table Web Scraper (Flask)

Scrape **all occurrences** of any HTML tag from a webpage. Special support for **tables**:

- CSV → all tables, 2-row gap between each
- Excel → each table in a separate sheet
- PDF → each table separate with heading

Handles **non-table tags** and **no-internet gracefully**.


